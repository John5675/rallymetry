# Training

The custom pickleball detector milestone is current. Keep small, versioned experiment
configurations here; `ball-detector.example.json` is the reference schema. Run products
and checkpoints belong in ignored experiment/artifact directories, never Git.

Training configurations must pin a human dataset version, fixed split and annotation
manifests, model version, base model, resolution, seed, and deterministic setting.
