# Evaluation

Detector evaluation products are local and ignored. Compare inference strategies only
on identical fixed validation record IDs, then reserve the fixed test partition for a
final selected-model audit. Persist precision, recall, false positives, false
positives/minute, coverage, and near/far subtotals with every run.
