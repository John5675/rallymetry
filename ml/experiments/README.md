# ML Experiments

Use this area for local custom-detector runs. Each run must retain `experiment.json`,
`metrics.json`, prepared-dataset metadata, configuration and data hashes, model
version, code revision, seed, runtime versions, and weight hashes. Weights, plots,
prepared images/labels, and other large products remain ignored.
